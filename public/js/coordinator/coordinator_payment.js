$(document).ready(function () {
    $("#coordinatorPaymentForm").submit(function (e) {
        e.preventDefault();
        var routeUrl = $(this).data("route"); // Get the route URL from the data attribute
        var formData = $(this).serialize();
        $.ajax({
            url: routeUrl,
            type: "POST",
            data: formData,
            dataType: "json",
            headers: {
                "X-CSRF-TOKEN": $('meta[name="csrf-token"]').attr("content"),
            },
            success: function (data) {
                console.log(data);
                if (data.response == "success") {
                    toastr["success"](data.message);
                    toastr.options = {
                        closeButton: true,
                        progressBar: true,
                    };
                    $("#coordinatorPaymentModal").modal("hide");
                    $("#table_div").load(location.href + " #table_div>*", "");
                    $("#coordinatorPaymentForm")[0].reset();
                } else {
                    // Check if there are validation errors in the response
                    if (data.message && data.message.constructor === Object) {
                        // Iterate through the validation error messages
                        for (var key in data.message) {
                            if (data.message.hasOwnProperty(key)) {
                                toastr["error"](data.message[key][0]); // Display the first error for each field
                            }
                        }
                    } else {
                        toastr["error"](data.message); // Display other error messages
                    }
                    toastr.options = {
                        closeButton: true,
                        progressBar: true,
                    };
                }
            },
        });
    });
    /////////////////////////// admin confirm coordinator payment///////////////////////////////

    $(document).on("click", "#paymentConfirmButton", function (e) {
        e.preventDefault();
        var id = $(this).attr("value");
        var amount = $(this).data('amount');
        var coordinator_id = $(this).data('coordinator-id');
        var particulars = $(this).data("particulars");
        var paid_date = $(this).data("paid-date");
        $.ajax({
            type: "POST",
            url: paymentConfirmUrl,
            data: { id: id ,amount:amount,coordinator_id:coordinator_id,particulars:particulars,paid_date:paid_date},
            dataType: "json",
            headers: {
                "X-CSRF-TOKEN": $('meta[name="csrf-token"]').attr("content"),
            },
            success: function (data) {
                console.log(data);
                if (data.response == "success") {
                    toastr["success"](data.message);
                    toastr.options = {
                        closeButton: true,
                        progressBar: true,
                    };
                    $("#table_div").load(location.href + " #table_div>*", "");
                } else {
                    // Check if there are validation errors in the response
                    if (data.message && data.message.constructor === Object) {
                        // Iterate through the validation error messages
                        for (var key in data.message) {
                            if (data.message.hasOwnProperty(key)) {
                                toastr["error"](data.message[key][0]); // Display the first error for each field
                            }
                        }
                    } else {
                        toastr["error"](data.message); // Display other error messages
                    }
                    toastr.options = {
                        closeButton: true,
                        progressBar: true,
                    };
                }
            },
        });
    });

    /////////////////////////// admin edit coordinator payment///////////////////////////////

    $(document).on("click", "#paymentEditButton", function (e) {
        e.preventDefault();
        var id = $(this).attr("value");
        var coordinatorName = $(this).data("coordinator-name");
        var date = $(this).data("date");
        var amount = $(this).data("paid_amount");
        var particulars = $(this).data("particulars");

        $('#pe_coordinatorName').val(coordinatorName);
        $('#pe_date').val(date);
        $('#pe_particulars').val(particulars);
        $('#pe_amount').val(amount);
        $('#coordinator_payment_id').val(id)
        $('#paymentEditModal').modal("show");
    });

    // admin edit payment

    $("#paymentEditForm").submit(function (e) {
        e.preventDefault();
        var routeUrl = $(this).data("route"); // Get the route URL from the data attribute
        var formData = $(this).serialize();
        $.ajax({
            url: routeUrl,
            type: "POST",
            data: formData,
            dataType: "json",
            headers: {
                "X-CSRF-TOKEN": $('meta[name="csrf-token"]').attr("content"),
            },
            success: function (data) {
                console.log(data);
                if (data.response == "success") {
                    toastr["success"](data.message);
                    toastr.options = {
                        closeButton: true,
                        progressBar: true,
                    };
                    $("#paymentEditModal").modal("hide");
                    $("#table_div").load(location.href + " #table_div>*", "");
                } else {
                    // Check if there are validation errors in the response
                    if (data.message && data.message.constructor === Object) {
                        // Iterate through the validation error messages
                        for (var key in data.message) {
                            if (data.message.hasOwnProperty(key)) {
                                toastr["error"](data.message[key][0]); // Display the first error for each field
                            }
                        }
                    } else {
                        toastr["error"](data.message); // Display other error messages
                    }
                    toastr.options = {
                        closeButton: true,
                        progressBar: true,
                    };
                }
            },
        });
    });
    // admin add payment for coordinator
    $("#adminAddPaymentForm").submit(function (e) {
        e.preventDefault();
        var routeUrl = $(this).data("route"); // Get the route URL from the data attribute
        var formData = $(this).serialize();
        $.ajax({
            url: routeUrl,
            type: "POST",
            data: formData,
            dataType: "json",
            headers: {
                "X-CSRF-TOKEN": $('meta[name="csrf-token"]').attr("content"),
            },
            success: function (data) {
                console.log(data);
                if (data.response == "success") {
                    toastr["success"](data.message);
                    toastr.options = {
                        closeButton: true,
                        progressBar: true,
                    };
                    $("#adminAddPaymentModal").modal("hide");
                    $("#table_div").load(location.href + " #table_div>*", "");
                    $("#adminAddPaymentForm")[0].reset();

                } else {
                    // Check if there are validation errors in the response
                    if (data.message && data.message.constructor === Object) {
                        // Iterate through the validation error messages
                        for (var key in data.message) {
                            if (data.message.hasOwnProperty(key)) {
                                toastr["error"](data.message[key][0]); // Display the first error for each field
                            }
                        }
                    } else {
                        toastr["error"](data.message); // Display other error messages
                    }
                    toastr.options = {
                        closeButton: true,
                        progressBar: true,
                    };
                }
            },
        });
    });

    //////////////////////////// ledger /////////////////////////////////

     // fetch corresponding ledger based on coordinator
     $("#fetchCoordinatorLedgerForm").submit(function (e) {
        e.preventDefault();
        var routeUrl = $(this).data("route"); // Get the route URL from the data attribute
        var formData = $(this).serialize();
        $.ajax({
            type: "POST",
            url: routeUrl,
            data: formData,
            dataType: "json",
            headers: {
                "X-CSRF-TOKEN": $('meta[name="csrf-token"]').attr("content"),
            },
            success: function (data) {
                console.log(data.coordinator_name)
                if (data.response == "success") {
                   
                    $("#coordinatorLedgerData").html(data.result);
                    $("#coordinatorNameId").text(data.coordinator_name);
                    $("#coordinatorOutstandingId").text(data.coordinator_outstanding);

                } else {
                    // Check if there are validation errors in the response
                    if (data.message && data.message.constructor === Object) {
                        // Iterate through the validation error messages
                        for (var key in data.message) {
                            if (data.message.hasOwnProperty(key)) {
                                toastr["error"](data.message[key][0]); // Display the first error for each field
                            }
                        }
                    } else {
                        toastr["error"](data.message); // Display other error messages
                    }
                    toastr.options = {
                        closeButton: true,
                        progressBar: true,
                    };
                }
            },
        });
    });

    // end of document ready
});
